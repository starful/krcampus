# Campus topic CSV (moved)

`guide_topics`, `language_schools`, and `universities` lists live in **okadmin** `data/topic_banks/krcampus/`.

Other files in this folder (e.g. `longtail_topics_seed_20.csv`) are local seeds and are unchanged.
